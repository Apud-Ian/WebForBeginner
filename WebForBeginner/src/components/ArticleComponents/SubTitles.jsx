const subTitels = ({ subTitel }) => {
    return(
        <div id={subTitel} className="p-4 m-2 ">
            <h2 className="text-3xl">{subTitel}</h2>
        </div>
    )
}

export default subTitels;